# benchmarking/

**Contributors:** open to whoever picks this up — not locked to one person.

Covers:
- Common benchmark/results schema (build before final measurements exist)
- MC + NN conventional-compute runtime/throughput benchmarks
- Aggregation of all team outputs into comparable tables
- Plots: accuracy, latency/speedup, data movement, energy, throughput
- Central MC vs NN-CPU vs NN+IMC comparison
- Final figures for demo/video

Result schema:
method,mae,rmse,r2,latency_ms,throughput,data_movement_bytes,energy_estimate
