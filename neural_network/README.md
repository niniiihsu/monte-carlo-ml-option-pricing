# neural_network/

**Contributors:** open to whoever picks this up — not locked to one person.

Covers:
- NN loading/preprocessing pipeline (works on dummy CSV first)
- Train/val/test split
- Architecture: start 5 -> 64 -> 64 -> 1
- Training on final MC labels
- Metrics: MAE, RMSE, R-squared, inference latency, param count, model size

Handoff -> imc_mapping/: final layer dims/weights/precision assumptions
Handoff -> benchmarking/: accuracy + runtime results
